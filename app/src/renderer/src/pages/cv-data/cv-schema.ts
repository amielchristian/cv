/** Minimal types aligned with cvbuild / LaTeXGenerator section shapes. */

export type {
  AwardEntry,
  EducationEntry,
  ExperienceEntry,
  LeadershipEntry,
  Profile,
  ProfileLink,
  SkillGroup
} from '@db/cv-types'
