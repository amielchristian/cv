/** Shared chrome for editor / preview pane headers (aligned top bars). */
export const PANE_HEADER_ROW =
  'flex min-h-10 flex-shrink-0 items-center border-b border-[var(--border-scroll)] bg-muted px-4 py-0'
